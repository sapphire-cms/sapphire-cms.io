import {Docs} from "../../docs.types";

export const firebase_default: Docs = {
  "title": "Deploy on Firebase",
  "precedent": "docs/deployment/node:default",
  "next": "",
  "content": "Firebase is a popular serverless cloud platform that is fully supported as a runtime for Sapphire.\n\nIn this section, you will learn how to deploy and run both the Sapphire headless CMS and the Manager \nUI—its web-based interface for content management and administration—on Firebase.\n\n## Install\n\nTo add Firebase runtime support and required services to your Sapphire project, run:\n\n```shell\nsapphire-cms package install firebase aot\n```\n\nor\n\n```shell\nscms pkg i firebase aot\n```\n\n**Important**\n\nFirebase only supports running a bundled version of the CMS.\nTherefore, you must also install the `aot` compiler to build your project ahead of time before deployment.\n\n## Deploy Headless CMS on Firebase Functions\n\n### Step 1: Add configuration for AOT and Firebase plateform\n\nSapphire doesn't require any compilation by defaults, as its modules are auto-discovered and loaded during startup. But in Serverless environement,\nwhere cold start time matters, creting a bundle prior to deployment is a must.\n\nSapphire AOT compiler discovers used modules, and produces a single runnable, including all dependencies, configurations ans schemas.\n\nConfigure `aot` as following:\n\n```yaml\n# ./sapphire-cms.config.yaml\n\nconfig:\n  modules:\n    - module: aot\n      config:\n        dist: sapphire-functions/lib\n\nlayers:\n  platform: \"@firebase\"\n```\n\n### Step 2: Create subfolder for Sapphire function\n\nSapphire headless CMS will run as firebase function. To make it deployable, we need to place its binary in the subfolder\nof the project and point `firebase.json` to this subfolder.\n\n```\nMy-Project\n├── sapphire-functions\n    ├── package.json\n```\n\nContent of `package.json`:\n\n```json\n{\n  \"name\": \"sapphire-functions\",\n  \"type\": \"commonjs\",\n  \"main\": \"lib/sapphire-cms/sapphire.firebase.cjs\",\n  \"private\": true,\n  \"engines\": {\n    \"node\": \"22\"\n  },\n  \"dependencies\": {\n    \"firebase-functions\": \"^6.0.1\"\n  }\n}\n```\n\n### Step 3: Create deployable bundle\n\nCreate bundle with AOT by running:\n\n```shell\nscms build\n```\n\nIf your project is made with Node, you can declare a reusable task in `package.json` for this operation:\n\n```json\n{\n    \"scripts\": {\n      \"build:cms\": \"scms build\"\n    }\n}\n\nThen compile Sapphire with command:\n\n```shell\nnpm run build:cms\n```\n\nOnce bundle is produced, it should be placed into directory defined as `dist` of `aot` module configuration. In our case, it is `sapphire-functions/lib`:\n\n```\nMy-Project\n├── sapphire-functions\n    ├── package.json\n    ├── lib\n        ├── sapphire.firebase.cjs\n        ├── sapphire.firebase.js\n        ├── ...\n```\n\nNote: you don't need to commit files produced by `aot` so it is a  good idea to explude it from git or other source management system.\nExample of `.gitignore`:\n\n```\n# sapphire-functions/.gitignore\n\nnode_modules/\nlib\n```\n\n### Step 4: Declare CMS executable as firebase function\n\nTo do that, just add the following entry into `firebase.json` file:\n\n```json\n{\n  \"functions\": [\n    {\n      \"source\": \"sapphire-functions\",\n      \"codebase\": \"sapphire\",\n      \"ignore\": [\n        \"node_modules\"\n      ]\n    }\n  ]\n}\n```\n\n### Step 5: Deploy on Firebase\n\nSapphire now is ready to be deployed on Firebase. Do it with:\n\n```shell\nfirebase deploy --only functions\n```\n\nAfter that, verify that new function has appeared on Firebase console;"
};
