import {Docs} from "../../docs.types";

export const cloudinary_default: Docs = {
  "title": "Cloudinary",
  "precedent": "docs/integrations/github:default",
  "next": "docs/deployment/deployment-overview:default",
  "content": "Media are a fundamental part of any CMS. Documents often reference images, videos or audio files.\nRather than storing those binaries together with documents, Sapphire CMS delegates media management to a dedicated **media layer**.\n\nThe media layer is responsible for:\n\n- storing media binaries;\n- retrieving media content;\n- generating media URLs for delivery;\n- providing optimized renderings such as thumbnails or resized images.\n\nSapphire stores media metadata as ordinary documents in its **persistence layer**. Actual media binaries are managed by the configured media provider.\n\nThe cloudinary module provides a media layer implementation backed by [Cloudinary](https://cloudinary.com).\n\n## Install\n\n```shell\nsapphire-cms package install cloudinary\n```\n\nor\n\n```shell\nscms pkg i cloudinary\n```\n\n## Provided Layers\n\n- `media`\n\n## Configuration\n\n```yaml\n# ./sapphire-cms.config.yaml\n\nconfig:\n  modules:\n    - module: cloudinary\n      config:\n        cloud-name: ${env.CLOUDINARY_CLOUD_NAME}\n        api-key: ${env.CLOUDINARY_API_KEY}\n        api-secret: ${env.CLOUDINARY_API_SECRET}\n\nlayers:\n  media: '@cloudinary'\n```\n\n### Parameters\n\n| Parameter  | Type   | Mandatory | Description                    |\n| ---------- | ------ | --------- | -------------------------------|\n| cloud-name | string | yes       | Name of your Cloudinary cloud. |\n| api-key    | string | yes       | Cloudinary API key.            |\n| api-secret | string | yes       | Cloudinary API secret.         |\n\nTo obtain these parameters, sign in to your Cloudinary account and navigate to ⚙️ **Settings → API Keys**.\n\nYour **Cloud Name** is displayed at the top of the page, to the right of the page title.\n\nTo create an API key, click **Generate New API Key**.\n\nGive the key a descriptive name and assign it the **Media Library User** role.\n\nOnce the key has been created, click the 👁️ icon to reveal the API secret.\n\nCopy the **API Key** and **API Secret** into the environment variables of your deployment platform.\n\n> [!WARNING]\n> **Security warning**: Never store secrets directly in your Sapphire CMS configuration file.\n> Instead, keep sensitive values in environment variables or your platform's secret management system, \n> and reference them using environment variable interpolation.\n"
};
