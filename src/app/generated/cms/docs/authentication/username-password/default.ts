import {Docs} from "../../docs.types";

export const usernamePassword_default: Docs = {
  "title": "Username & Password  ",
  "precedent": "docs/content-management/manager-ui:default",
  "next": "docs/integrations/overview:default",
  "content": "Authentication is a fundamental aspect of any software system, and Sapphire is no exception. If your CMS instance is exposed to the internet, \naccess **must** be properly secured.\n\nSapphire follows a modular architecture, which means it does not include a security layer by default. Instead, authentication is \nadded by installing the module that best fits your needs.\n\nThe simplest option is the `simple-auth` module.\n\nThis module provides basic authentication using a username and password. Any user authenticated through `simple-auth` \nis granted **administrator privileges**.\n\n## Install\n\nTo enable simple username/password authentication in your project, run:\n\n```shell\nsapphire-cms package install simple-auth\n```\n\nor \n\n```shell\nscms pkg i simple-auth\n```\n\n## Configuration\n\nConfigure the administrator credentials in the `sapphire-cms.config.yaml` file as shown below.\n\n**Security warning**\n\nNever store credentials in plain text within your configuration files. This introduces serious security risks, especially if your CMS is exposed to the internet.\nAlways use environment variables or platform-specific secrets to manage sensitive data.\n\n\n```yaml\n# ./sapphire-cms.config.yaml\n\nconfig:\n  modules:\n    - module: simple-auth\n      config:\n        username: ${env.SAPPHIRE_USERNAME}\n        password: ${env.SAPPHIRE_PASSWORD}\n\nlayers:\n  security: '@simple-auth'\n```\n\n**Notes:**\n\n- `${env.SAPPHIRE_USERNAME}` and `${env.SAPPHIRE_PASSWORD}` are resolved at runtime from environment variables.\n- Make sure these variables are properly configured in your hosting platform (cloud provider or local environment).\n\n### Module parameters\n\n| Parameter | Type   | Mandatory | Description              |\n| --------- | ------ | --------- | ------------------------ |\n| username  | string | yes       | Administrators username. |\n| password  | string | yes       | Administrators password. |\n"
};
